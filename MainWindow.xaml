<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Title="Калькулятор" Height="350" Width="420">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Первое число:" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="FirstNumberTextBox"
                 Grid.Row="1"
                 Height="30"
                 Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Второе число:" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="SecondNumberTextBox"
                 Grid.Row="3"
                 Height="30"
                 Margin="0,0,0,15"/>

        <WrapPanel Grid.Row="4" HorizontalAlignment="Center" Margin="0,0,0,15">
            <Button x:Name="AddButton"
        Content="+"
        Width="60"
        Height="35"
        Margin="5"
        Click="AddButton_Click"/>

            <Button x:Name="SubtractButton"
                    Content="-"
                    Width="60"
                    Height="35"
                    Margin="5"/>

            <Button x:Name="MultiplyButton"
                    Content="*"
                    Width="60"
                    Height="35"
                    Margin="5"/>

            <Button x:Name="DivideButton"
                    Content="/"
                    Width="60"
                    Height="35"
                    Margin="5"/>
        </WrapPanel>

        <StackPanel Grid.Row="5">
            <TextBlock Text="Результат:" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock x:Name="ResultTextBlock"
                       Text="Здесь будет результат"
                       FontSize="18"/>
        </StackPanel>
    </Grid>
</Window>